{
  "id": "bugfix_leaky_limiter",
  "status": "draft",
  "category": "bugfix",
  "description": "Fix a rate limiter that resets the counter instead of the window on first request.",
  "base_commit": "none",
  "task": "Fix the rate limiter so that a new client IP is counted correctly instead of resetting the window.",
  "draft_reason": "The repository snapshot is a placeholder; no independently replayable case exists yet."
}
