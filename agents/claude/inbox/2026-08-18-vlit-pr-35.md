# Влить pull request #35 и обновить main

- **Кому:** claude
- **Дата:** 2026-08-18
- **От кого:** владелец проекта
- **Ветка:** `chore/agents-workspace` (влита)
- **Файл отчёта:** `../done/2026-08-18-vlit-pr-35.md`

## Что нужно сделать

Влить открытый pull request #35 «chore(agents): единая структура заданий и
приёмки» в `main`, обновить локальный `main`, удалить слитую ветку.

Задание записано после выполнения осознанно: структура `agents/` появлялась в
репозитории именно этим pull request, и до слияния файл задания было некуда
положить. Расхождение зафиксировано здесь, чтобы не выглядеть работой без
задания.

## Границы

Только слияние и обновление ветки. Содержимое pull request не править, новых
изменений в него не добавлять.

## Как проверить результат

- `gh pr view 35 --json state,mergeable,mergeStateStatus` до слияния —
  состояние `OPEN`, `MERGEABLE`, `CLEAN`;
- `gh pr checks 35` — все проверки `pass`;
- `git log --oneline -1` после слияния — коммит слияния в `main`.

## Контекст и ограничения

- `AGENTS.md` §5: прямой push в `main` запрещён, результат оформляется
  pull request; принимает работу и пушит главный агент;
- `AGENTS.md` §3: красная сборка основной ветки — инцидент, слияние допустимо
  только по зелёным проверкам.
