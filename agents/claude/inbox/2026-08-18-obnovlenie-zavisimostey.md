# Обновить зависимости

- **Кому:** claude
- **Дата:** 2026-08-18
- **От кого:** владелец проекта
- **Ветка:** `claude/zavisimosti-2026-08-18`
- **Файл отчёта:** `../done/2026-08-18-obnovlenie-zavisimostey.md`

## Что нужно сделать

Свести зависимости к текущему состоянию: `pnpm-lock.yaml` для рабочего
пространства Node и `Cargo.lock` для `services/gateway`. Сначала собрать список
устаревшего с указанием текущей и доступной версии, затем поднять то, что
проходит проверки.

Отдельной веткой и отдельным pull request: обновление зависимостей не
смешивается с перекладыванием заданий, иначе ни отревьюить, ни откатить.

## Границы

Обновление версий существующих зависимостей. Не добавлять новые зависимости,
не заменять библиотеки и не менять стек — это отдельное решение владельца, а не
побочный эффект обновления. Мажорные подъёмы, требующие правки кода, в эту
задачу не входят: перечислить их в отчёте отдельным списком.

## Как проверить результат

- `pnpm install --frozen-lockfile=false` — success;
- `pnpm check` — success;
- `cargo fmt --manifest-path services/gateway/Cargo.toml --check` — success;
- `cargo clippy --manifest-path services/gateway/Cargo.toml --all-targets -- -D warnings` — success;
- `cargo test --manifest-path services/gateway/Cargo.toml` — success.

Вывод команд приводится в отчёте фактическим текстом.

## Контекст и ограничения

- `AGENTS.md` §4: изменение зависимости требует явного обоснования в отчёте
  с указанием лицензии;
- `AGENTS.md` §10: файлы блокировки порождаются менеджером пакетов и не
  правятся вручную;
- `package.json`: `packageManager` закреплён на `pnpm@10.14.0`, Node `>=22`.
