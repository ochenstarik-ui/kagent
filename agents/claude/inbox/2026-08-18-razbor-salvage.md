# Разобрать agents/_salvage-2026-08-18

- **Кому:** claude
- **Дата:** 2026-08-18
- **От кого:** владелец проекта
- **Ветка:** `claude/aktualizaciya-2026-08-18`
- **Файл отчёта:** `../done/2026-08-18-razbor-salvage.md`

## Что нужно сделать

`agents/_salvage-2026-08-18/` — перенесённая с диска папка `kagent-tasks`
(87 файлов), автора которых не удалось определить по имени файла. Разобрать её:
для каждого задания определить, закрыто ли оно уже в `main`, и развести
разобранное по назначению — в `inbox/` соответствующего агента, если работа
осталась, либо в архив приёмки, если работа закрыта.

Признак закрытия — артефакт: коммит в `main`, слитый pull request, зелёный
прогон непрерывной интеграции. Не «выглядит сделанным».

## Границы

Только перемещение и классификация файлов заданий. Никакого кода служб,
никаких правок `docs/`, никаких новых заданий по существу найденного. Задания,
принадлежность которых определить не удалось, остаются в `_salvage-2026-08-18/`
с объяснением в отчёте — это честнее, чем угаданный адресат.

## Как проверить результат

- `git status --short` — только перемещения внутри `agents/`;
- в отчёте таблица: файл — вердикт (закрыто / осталось / не определено) —
  артефакт, обосновывающий вердикт;
- `python scripts/validate_repository.py` — success.

## Контекст и ограничения

- `agents/README.md`: маршрут задания и правила `_accepted/`;
- `AGENTS.md` §2: чужая область не трогается даже попутно;
- `agents/README.md`: удалять принятое из `_accepted/` нельзя.
