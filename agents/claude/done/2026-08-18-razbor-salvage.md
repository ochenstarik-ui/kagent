# Отчёт: разобрать agents/_salvage-2026-08-18

- **Задание:** `../inbox/2026-08-18-razbor-salvage.md`
- **Агент:** claude
- **Дата:** 2026-08-18
- **Ветка / коммиты:** `claude/aktualizaciya-2026-08-18`, `d5222fe`

## Что сделано

Разобраны все 87 файлов из `agents/_salvage-2026-08-18/`, папка удалена.

**Найденный дефект переноса.** Коммит `997495f` раскладывал файлы по префиксу имени,
игнорируя папку-источник. В исходной папке `kagent-tasks` семантику несла именно папка:
`Старые задачи/` — выполненное, `Поглощённые/` — закрытое другой работой, `Отложенные/` —
не сделанное, `Справочное/` — не задания. В результате 31 выполненная постановка лежала
в `inbox/` Hermes и Antigravity как работа к исполнению, а четыре активные — в `done/`
как выполненные. Раскладка восстановлена по вердикту.

**Закрытая работа** — в `agents/_accepted/2026-08-18-arhiv-kagent-tasks/`, сгруппирована
по единице приёмки (pull request), 11 папок; рядом отчёты исполнителей с доказательствами
(`TEST_EVIDENCE.md`, `CI_CHECKS.txt`, `SHA256SUMS`), пришедшие тем же переносом.
Таблица «постановка → pull request» — в `INDEX.md` архива.

| Папка | Постановки | Артефакт |
|---|---|---|
| `0.9.0-green-trunk-pr03/` | 0.9.0, A1–A7, A9, A8 | PR #3 |
| `0.9.1-measurability-pr04-pr06/` | 0.9.1, B1, B2, B4, B3, B5 | PR #4, #6 |
| `c1-c5-control-plane-pr07-pr10/` | C1–C5 | PR #7, #8, #9, #10 |
| `d1-e1-e4-runtime-perimeter-pr11-pr15/` | D1, E1–E4 | PR #11, #14, #15 |
| `p1-p9-d4-d5-vertical-pr17/` | P1, P2, P3, P8, P9, D4, D5, P4, D2, D3 | PR #17 |
| `p10-p12-runtime-web-pr18/` | P10, P11, P12, P5, P6, P7 | PR #18 |
| `e6-e10-pr21-pr27/` | E6, E7, E9, E10, E5 | PR #21, #22, #23, #26, #27 |
| `e11-image-build-gate-pr29/` | E11 | PR #29 |
| `e13-verification-state-pr30/` | E13 | PR #30 |
| `e8-deployment-smoke-pr31/` | E8 | PR #31 |
| `p13-p14-server-ready-pr34/` | P13, P14 | PR #34 |

**Незакрытая работа** — в очереди агентов:

| Что | Куда |
|---|---|
| Этапы 0.9.2–0.9.5, ни разу не выполнявшиеся | `agents/hermes/inbox/отложенные-этапы/` |
| E14 — программа закрытия отложенных этапов | `agents/hermes/inbox/hermes-e14-program-deferred-stages.md` |
| E15 — бюджетный ledger и кэш | `agents/hermes/inbox/hermes-e15-budget-ledger-and-cache.md` |
| E16 — план распараллеливания | `agents/hermes/inbox/hermes-e16-parallel-tracks.md` |
| X1 — разбор закрытого PR #1 | `agents/antigravity/inbox/antigravity-x1-analyze-pr1.md` |

**Прочее.** `вытесненные/hermes-e12-budget-ledger.md` — слитого pull request нет,
предмет перенесён в активную E15. `справочное/` — 17 файлов, заданиями не являющихся:
правила старой папки, протокол воркера Hermes, аудит 2026-08-10, разбор PR #1,
переписка агентов по B4, C3, C5.

## Как проверено

Ни один файл не потерян:

```
$ git diff --cached --diff-filter=D --name-only | grep -v '\.gitkeep'
(пусто)

было:  salvage 87 + hermes 18 + antigravity 19 = 124
стало: _accepted 116 (включая новый INDEX.md) + hermes/antigravity 9 = 125
```

Критерий приёмки:

```
$ python scripts/validate_repository.py
Repository validation passed: 9 required files present
```

Вердикт «закрыто» опирался на слитый pull request:

```
$ gh pr list --state merged --limit 50
35, 34, 33, 32, 31, 30, 29, 27, 26, 23, 22, 21, 19, 18, 17, 16, 15, 14, 11, 10, 9, 8, 7, 6, 4, 3, 2
```

Вердикт «поглощено» и «отложено» взят из документов, написанных при постановке задач:
`справочное/README-poglosjennye.md` и `отложенные-этапы/README.md`.

## Что не сделано

Ничего из задания.

Оговорка по P13: собственный PR #20 закрыт неслитым, но предмет постановки в `main`
присутствует — `apps/web/e2e/dashboard.spec.ts`, `apps/web/playwright.config.ts`, зелёный
job `e2e`. Работа дошла до `main` другим путём, постановка отнесена к закрытым.

Оговорка по E12: в таблице поглощённых постановок строки не было. Вердикт восстановлен
по `справочное/README-kagent-tasks.md`, где E15 описана как «линия A: бюджетный ledger».
Это единственное место, где вердикт опирается не на слитый pull request.

## Замечено рядом

1. **Непрерывная интеграция не запускает `pnpm lint` и `pnpm format:check`.** Job `node`
   в `.github/workflows/ci.yml` выполняет только `pnpm typecheck`, `pnpm test`, `pnpm build`.
   При этом `AGENTS.md` §6 и `Makefile` предъявляют `pnpm check`, куда lint входит.
   Подробности и текущее падение — в отчёте по зависимостям.
2. **`agents/README.md` не описывает места для справочного материала.** Схема знает
   `inbox/`, `done/`, `notes/`, `_accepted/`, но не знает, куда класть протоколы и аудиты,
   заданиями не являющиеся. Сейчас они лежат в `справочное/` внутри архива приёмки —
   решение принято мной, схемой не предусмотрено.

## Вопросы к приёмке

Группировка архива по pull request, а не по одной папке на постановку, выбрана мной:
единица приёмки здесь — именно pull request, и 11 папок читаются лучше сорока.
Если принят другой принцип, перекладывание дешёвое — файлы не менялись, только перемещались.
